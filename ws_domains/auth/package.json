{
  "name": "@repo-domains/auth",
  "version": "0.1.0",
  "description": "Authentication domain for the monorepo",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": ["dist/**"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["auth", "domain", "ddd"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@repo-tooling/tsup": "workspace:*",
    "@repo-tooling/typescript": "workspace:*",
    "@repo-tooling/vitest": "workspace:*",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@repo-domains/domain-core": "workspace:*"
  }
}
