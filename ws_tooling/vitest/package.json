{
  "name": "@repo-tooling/vitest",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Vitest configuration for the monorepo",
  "main": "index.js",
  "files": ["dist/**"],
  "scripts": {
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "vitest": "^3.0.8"
  }
}
