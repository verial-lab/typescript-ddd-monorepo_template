{
  "name": "@repo-tooling/tsup",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tsup configuration for the monorepo",
  "main": "index.js",
  "files": ["dist/**"],
  "scripts": {
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "tsup": "^8.4.0"
  }
}
